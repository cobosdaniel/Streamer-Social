{
  "rewrites": [
    { "source": "/api/:path*",  "destination": "$BACKEND_URL/api/:path*"  },
    { "source": "/auth/:path*", "destination": "$BACKEND_URL/auth/:path*" },
    { "source": "/(.*)",        "destination": "/"                        }
  ]
}
